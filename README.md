# 🎓 Placement Predictor Web App

An interactive Flask-based web application that predicts whether a student is likely to get placed based on their **IQ** and **CGPA** using a trained Machine Learning model.

---

## 🚀 Features
- Simple and user-friendly web interface  
- Takes **IQ** and **CGPA** as input  
- Predicts placement result (`Placed` / `Not Placed`) using a trained ML model  
- Built with **Flask**, **Scikit-learn**, and **NumPy**

---

## 🗂️ Project Structure
placement_predictor/
│
├── app.py # Flask backend
├── model.pkl # Trained ML model (add your own)
├── requirements.txt # Dependencies
├── templates/
│ └── index.html # Frontend HTML file
└── static/
└── style.css # Optional CSS styling